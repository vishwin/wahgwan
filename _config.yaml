defaults:
        -
                scope:
                        path: ""
                values:
                        layout: "base"

sass:
        style: compressed

host: ::1
permalink: pretty
show_dir_listing: false

plugins:
        - jekyll-assets
